build:
  gpu: true
  python_version: "3.10"
  system_packages:
    - git
  python_packages:
    - torch
    - transformers
    - accelerate
    - peft
    - safetensors

predict: "predict.py:Predictor"
